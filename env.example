# DGX Cluster Configuration
# Copy this to .env and customize for each node

# Node role: head or worker
ROLE=head

# ConnectX-7 interface name (use 00-detect-ifaces.sh to find yours)
IFACE=cx70

# Point-to-point IP addresses (/30 network)
# Head: 192.168.40.1, Worker: 192.168.40.2
IP=192.168.40.1
PEER_IP=192.168.40.2

# Ray cluster coordination
MASTER_ADDR=192.168.40.1
MASTER_PORT=6379

# vLLM API server
VLLM_PORT=8000

# Model to serve (change to your preferred model)
MODEL=Qwen/Qwen2.5-72B-Instruct

# Tensor parallel size (number of GPUs across cluster)
TP_SIZE=2

# NCCL configuration (optional overrides)
NCCL_DEBUG=INFO
NCCL_IB_DISABLE=0
NCCL_IB_HCA=mlx5

# HuggingFace cache directories (optional)
# HF_HOME=$HOME/.cache/huggingface
# TRANSFORMERS_CACHE=$HF_HOME

